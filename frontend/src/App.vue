<template>
  <!-- Main application wrapper using Vuetify's v-app component -->
  <v-app>
    <!-- Render the current route's component -->
    <router-view />
  </v-app>
</template>

<script lang="ts" setup>
  /**
   * Main Application Component
   * 
   * This is the root component of the ITAPIA application.
   * It serves as the main wrapper that includes the Vuetify application
   * and renders the current route's component via router-view.
   */
</script>
